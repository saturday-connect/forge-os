name: Build Desktop App

on:
  push:
    branches: [main]
    tags: ['v*']
  workflow_dispatch:

jobs:
  build:
    strategy:
      fail-fast: false
      matrix:
        include:
          - os: macos-latest
            platform: mac
            artifact: "*.dmg"
          - os: windows-latest
            platform: win
            artifact: "*.exe"

    runs-on: ${{ matrix.os }}

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up Python 3.12
        uses: actions/setup-python@v5
        with:
          python-version: '3.12'

      - name: Build forge binary
        run: python3 src/build_forge.py

      - name: Set up Node.js 20
        uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: 'npm'
          cache-dependency-path: electron/package.json

      - name: Install Electron dependencies
        run: npm install
        working-directory: electron

      - name: Build desktop app (macOS)
        if: runner.os == 'macOS'
        run: npm run build:mac -- --publish ${{ startsWith(github.ref, 'refs/tags/') && 'always' || 'never' }}
        working-directory: electron
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          # TODO: add CSC_LINK and CSC_KEY_PASSWORD secrets for code signing before public release

      - name: Build desktop app (Windows)
        if: runner.os == 'Windows'
        run: npm run build:win -- --publish ${{ startsWith(github.ref, 'refs/tags/') && 'always' || 'never' }}
        working-directory: electron
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          # TODO: add WIN_CSC_LINK and WIN_CSC_KEY_PASSWORD for Authenticode signing

      - name: Upload build artifact
        if: ${{ !startsWith(github.ref, 'refs/tags/') }}
        uses: actions/upload-artifact@v4
        with:
          name: forge-os-${{ runner.os }}
          retention-days: 14
          path: |
            electron/dist/${{ matrix.artifact }}
            electron/dist/*.zip
            electron/dist/latest*.yml
